# AI Financial Advisor
## Product, Development & Vibe-Coding Master Plan

**Purpose:** Build a privacy-first, self-hosted household financial intelligence platform that provides researched, challengeable financial guidance through a simple conversational experience.

**Primary deployment model:** Docker / Docker Compose, self-hosted.

**Primary users:** Two-person household; both spouses have first-class identities and access to shared household financial information.

**Core principle:** The product is a financial decision platform with an AI interface—not an LLM with access to raw financial data.

---

# 1. Product Vision

The AI Financial Advisor should become a **household financial operating system** that maintains a current financial state based on the latest available imported data and helps answer questions such as:

- How are we doing financially?
- Help me create an initial budget.
- Why am I always over budget?
- Help me revise the budget for next month with these known activities.
- Can we afford this?
- What should we do with this unexpected expense?
- What should we do with this unexpected money?
- Which credit card should I use?
- Are our savings goals on track?
- Are we on track for financial independence?

The system should remember the household's structured financial state so the user does not have to restate it in every conversation.

The system should also make it easy for a non-technical spouse to:

- upload statements;
- resolve an error;
- understand the household's financial position;
- understand why a recommendation was made;
- approve or reject a proposed financial plan.

---

# 2. Core Architectural Principles

## 2.1 Financial truth is deterministic

Balances, budgets, cash flow, debt, goal progress, scenario calculations, financial independence projections, and other material calculations must be produced by deterministic application services—not generated by the LLM.

## 2.2 The LLM is a reasoning and communication interface

The AI should:

- understand natural language;
- select approved tools;
- explain structured financial results;
- compare alternatives;
- manage conversational context;
- assist with planning workflows;
- summarize validated research.

The AI should not be the system of record.

## 2.3 Minimum data exposure

Raw financial records remain inside the self-hosted environment by default.

External LLM calls must pass through a privacy gateway that:

- minimizes context;
- removes restricted identifiers;
- blocks secrets;
- prevents raw statement leakage;
- allowlists external destinations.

## 2.4 Tool-only AI access

The AI must never receive unrestricted database, filesystem, shell, or network access.

All financial access occurs through typed, authorization-aware tools.

## 2.5 Explainability

Material financial outputs should distinguish:

- Fact
- Calculation
- Assumption
- Analysis
- Proposal
- Approved decision

## 2.6 Explicit approval

The AI can propose a budget, allocation, scenario, or other plan, but it cannot silently change household financial state.

## 2.7 Exceptions instead of perfection

Normal processing should be automated. Ambiguity should move to a Review Queue instead of forcing the whole workflow to fail.

## 2.8 UX simplicity

The default UI should be understandable by a non-technical spouse without understanding:

- APIs;
- OCR;
- bank/utility/loan/credit-card statement processing;
- reconciliation;
- AI orchestration;
- Docker;
- cloud infrastructure.

---

# 3. Product Data-Ingestion Strategy & Product Decision

## Online institution integrations are postponed — date TBD

The product will **not implement direct online connections to banks, credit-card issuers, loan providers, utilities, brokerages, or other financial institutions during the initial roadmap**. The timing for online institution integration is **TBD** and will be revisited only after the document/CSV ingestion experience, financial normalization, reconciliation, and household UX have proven reliable.

This is an intentional Product Owner decision, not an architectural dead end. The application will retain a provider-neutral ingestion boundary so online integrations can be added later without changing the canonical financial model.

### Current data-ingestion priority

The near-term product priority is to make it extremely easy for a household member to:

1. Export financial data from an institution as CSV and import it.
2. Download a PDF statement/bill and upload it.
3. Have the system identify the source type automatically.
4. Extract balances, transactions, charges, payments, due dates, recurring obligations, and other relevant financial facts.
5. Reconcile the imported information against previously imported data.
6. Resolve only the exceptions that require human judgment.
7. Recalculate the household financial state automatically.

### Supported source categories

The ingestion pipeline should explicitly support these source categories:

| Source | Typical files | Primary extracted information |
|---|---|---|
| Bank | CSV, PDF | account balance, deposits, withdrawals, transfers, transactions, statement period |
| Credit Card | CSV, PDF | purchases, payments, credits, fees, statement balance, minimum payment, due date, rewards-related observations where present |
| Loan | CSV, PDF | principal balance, payment, interest rate when shown, fees, due date, payment history, payoff/term information when shown |
| Utility | CSV, PDF | provider, service period, bill amount, due date, recurring charge, usage where available, taxes/fees, payment status |

Utility documents are **obligations/bills**, not necessarily financial accounts. Their extracted data should feed the recurring-obligation and cash-flow models rather than force them into the Account model.

### Product principle

> **Make manual import feel automatic before making automatic connections available.**

The application should achieve a high-quality, low-friction import and reconciliation experience first.

---

# 3. Target Product Architecture

```text
                         ┌───────────────────────────┐
                         │       WEB / PWA UI        │
                         │                           │
                         │ Dashboard • Chat         │
                         │ Budget • Goals           │
                         │ Review • Accounts        │
                         └─────────────┬─────────────┘
                                       │
                                       ▼
                         ┌───────────────────────────┐
                         │       EXPERIENCE API      │
                         └─────────────┬─────────────┘
                                       │
              ┌────────────────────────┼────────────────────────┐
              │                        │                        │
              ▼                        ▼                        ▼
      ┌──────────────┐       ┌────────────────┐       ┌───────────────┐
      │ AI Advisor   │       │ Financial      │       │ Ingestion     │
      │ Orchestrator │       │ Intelligence   │       │ Platform      │
      └──────┬───────┘       └───────┬────────┘       └──────┬────────┘
             │                        │                       │
             ▼                        ▼                       ▼
      Context Builder          Financial Engine        Documents
      Tool Layer               Budget                  CSV / PDF Financial Data
      Workflows                Cash Flow               CSV / PDF
      Privacy Gateway          Goals                   Reconcile
      Response Validation      Debt
                               FI
             │                        │
             └──────────────┬─────────┘
                            ▼
                   ┌──────────────────┐
                   │ Financial State  │
                   │ / Snapshot       │
                   └────────┬─────────┘
                            │
                  ┌─────────┴─────────┐
                  ▼                   ▼
             PostgreSQL             MinIO
             structured truth       source documents
                  │
                  ▼
             Redis / Worker

                      PRIVACY BOUNDARY
                            │
                            ▼
                       Public LLMs
```

---

# 4. Development Strategy

## 4.1 Use vertical slices

Do not build the application strictly by technical layer.

Each slice should produce a working user-visible capability.

```text
Slice
  ↓
Domain capability
  ↓
API capability
  ↓
UI capability
  ↓
Tests
  ↓
UX validation
  ↓
Architecture review
```

## 4.2 Use a modular monolith first

The target architecture contains logical service boundaries, but the rapid prototype should not begin as a collection of 15 microservices.

Start with approximately:

```text
web
api
worker
postgres
redis
minio
keycloak
reverse-proxy
```

Maintain logical modules so they can later be extracted:

```text
api/modules/
  household
  accounts
  transactions
  statements
  reconciliation
  budgets
  goals
  financial-intelligence
  advisor
  recommendations
  research
  validation
```

## 4.3 One large prompt + targeted prompts

For every slice use:

1. One **slice-scoping/master prompt** that establishes the objective, boundaries, architecture, UX expectations, and definition of done.
2. Several **targeted prompts**, each implementing one coherent capability.
3. One **integration prompt**.
4. One **architecture / financial correctness / security / UX review prompt**.
5. Targeted repair prompts for selected review findings.

Do not use one huge prompt to build an entire slice.

## 4.4 Coding-agent operating rule

The agent should optimize for preserving architecture while completing the current slice—not simply for completing the current prompt.

---

# 5. Repository Structure

```text
financial-advisor/
├── apps/
│   ├── web/
│   ├── api/
│   └── worker/
├── packages/
│   ├── domain/
│   ├── contracts/
│   ├── db/
│   ├── financial/
│   ├── ai/
│   ├── security/
│   ├── ui/
│   └── shared/
├── infra/
│   ├── docker/
│   ├── keycloak/
│   ├── postgres/
│   └── backup/
├── fixtures/
│   ├── households/
│   ├── transactions/
│   ├── statements/
│   └── scenarios/
├── tests/
│   ├── unit/
│   ├── integration/
│   ├── e2e/
│   ├── financial/
│   ├── privacy/
│   └── ux-flows/
├── docs/
│   ├── architecture/
│   ├── adr/
│   ├── product/
│   └── runbooks/
├── AGENTS.md
├── PRODUCT_BUILD_CONTRACT.md
├── docker-compose.yml
└── README.md
```

---

# 6. Required Agent Governance Files

Before building features, create and maintain:

## `AGENTS.md`

Permanent engineering rules:

- financial calculations are deterministic;
- money never uses floating point;
- LLM tool-only access;
- no raw SQL for AI;
- no sensitive financial data in normal logs;
- no external LLM access outside privacy gateway;
- no direct money movement;
- explicit approval for financial state changes;
- preserve source provenance;
- use Review Queue for uncertainty;
- non-technical spouse UX is a hard requirement;
- financial code requires tests;
- architecture drift must be reported.

## `PRODUCT_BUILD_CONTRACT.md`

Contains:

- product principles;
- user journeys;
- domain invariants;
- privacy rules;
- AI behavior rules;
- UX acceptance criteria;
- current MVP scope;
- out-of-scope items;
- release gates.

---

# 7. Vibe-Coding Rules

For every coding task:

1. Read `AGENTS.md`.
2. Read `PRODUCT_BUILD_CONTRACT.md`.
3. Read the relevant slice specification.
4. Inspect current implementation.
5. Identify conflicts before coding.
6. Propose the smallest coherent implementation.
7. Write or update tests.
8. Implement.
9. Run type checking.
10. Run linting.
11. Run relevant tests.
12. Run integration/E2E tests when applicable.
13. Report architectural deviations.

Avoid prompts such as:

> Build the financial advisor.

Prefer:

> Implement `plan_next_month_budget()` using the existing budget domain service. Do not duplicate financial calculations in the AI layer. Add contract tests and preserve the existing FinancialSnapshot contract.

---

# 8. Slice Roadmap

| Slice | Purpose | Major Output |
|---|---|---|
| 1 | Household Financial Pulse | Household + accounts + FinancialSnapshot + dashboard |
| 2 | Financial Statement & Bill Ingestion | CSV/PDF extraction for bank, credit-card, loan, and utility data + reconciliation + Review Queue |
| 3 | Financial Intelligence | Budget + cash flow + goals + debt + recurring obligations + attention |
| 4 | AI Advisor & Planning | Conversational AI + budget planning workflows |
| 5 | Research & Validated Recommendations | Evidence + recommendation + independent validation |
| 6 | Advanced Import Intelligence | Improve CSV/PDF extraction, source templates, document learning, reconciliation, and recurring-financial-fact extraction |
| 7 | Advanced Financial Optimization | Windfalls + surprise expenses + credit cards + FI |
| 8 | Privacy/Security/UX Hardening | Production-grade privacy, resilience, usability |
| 9 | Productionization | Backup/restore, observability, deployment, upgrade/runbooks |
| 10 | Online Institution Integrations — TBD | Direct institution/aggregator connections and automatic synchronization, timing TBD |

**Product sequencing decision:** Slices 1–9 are the active product-development roadmap. Slice 10 is deliberately postponed with a **TBD** date. No active MVP work should depend on direct institution integrations. The canonical ingestion model must remain provider-neutral so this capability can be added later without redesigning the financial domain.

---

# 9. Slice 1 — Household Financial Pulse

## Objective

Create household → add members → add fictional accounts → calculate FinancialSnapshot → display Financial Pulse.

## Exclusions

No online institution integration, statement ingestion, AI, research, credit-card optimization, investment recommendations, or money movement.

## Initial Slice Prompt

```text
Implement Slice 1 — Household Financial Pulse.

Build the first vertical slice of the AI Financial Advisor:

Create household → add household members → add fictional accounts → calculate a deterministic FinancialSnapshot → display a simple Household Financial Pulse.

Architecture:
- React/TypeScript web application
- Node/TypeScript API
- PostgreSQL
- Keycloak/OIDC
- Modular monolith
- Financial calculations in domain services

Implement:
- Household
- HouseholdMember
- Account
- Money value object
- FinancialSnapshot
- household API
- account API
- Financial Pulse dashboard

Use fictional seeded data.

Do not implement:
- online institution integrations
- statement ingestion
- AI
- research
- investment advice
- money movement

Non-negotiable rules:
- no floating point money calculations;
- UI contains no financial calculations;
- FinancialSnapshot is derived, not manually edited;
- current financial state is authoritative over presentation text;
- non-technical spouse must understand the dashboard within 30 seconds.

Before coding inspect the repository and report conflicts.

Definition of done:
- Docker environment starts
- authentication works
- household and accounts persist
- FinancialSnapshot calculates deterministically
- Financial Pulse displays correct values
- unit tests exist for financial calculations
- E2E covers login to dashboard
- no speculative AI or microservices are introduced
```

## Targeted Prompt 1 — Domain

```text
Implement Slice 1 domain contracts and persistence only.

Implement:
- Household
- HouseholdMember
- Account
- Money
- FinancialSnapshot

Create database migrations and repositories.

Money must not use floating point.
FinancialSnapshot is derived, not directly editable.

Seed a fictional Tucker Household.

Do not build UI, AI, online institution integration, or statement ingestion.

Add domain and persistence tests.
```

## Targeted Prompt 2 — Financial engine

```text
Implement the deterministic FinancialSnapshot engine for Slice 1.

Calculate:
- cash
- assets
- debt
- net worth
- monthly income
- monthly expenses
- monthly surplus
- financial health status

Use domain services, not controllers or UI.

Test normal and edge cases.

Expected seeded household results:
Cash = 19,200
Retirement = 410,000
Debt = 240,000
Net worth = 189,200
Monthly income = 12,000
Monthly expenses = 8,000
Monthly surplus = 4,000
```

## Targeted Prompt 3 — API

```text
Implement:
GET /household
GET /household/members
GET /accounts
POST /accounts
GET /financial-snapshot
GET /financial-pulse

Use existing domain services.
Do not duplicate financial calculations in controllers.
Validate schemas and enforce household authorization.
Add API integration tests.
```

## Targeted Prompt 4 — UX

```text
Implement the Slice 1 Financial Pulse UI.

The UI must answer:
- How are we doing?
- How much do we have?
- How much are we spending?
- Are we generating positive cash flow?
- Is anything requiring attention?

Show:
- household status
- net worth
- cash
- income
- expenses
- surplus
- debt
- account groups

Use plain language and progressive disclosure.
Do not calculate financial values in React.
Add Playwright coverage.
```

## Targeted Prompt 5 — Integration

```text
Integrate Slice 1 end to end:
Login → Household → Accounts → FinancialSnapshot → Financial Pulse.

Use real PostgreSQL data and real financial calculations.
Remove production-path mocks.
Verify authorization and seeded values.
Add E2E coverage.
```

## Slice 1 Review Prompt

```text
Perform a Slice 1 architecture, financial correctness, security, and UX review.

Do not add functionality.

Check:
- money representation
- financial calculation correctness
- domain/API/UI boundaries
- authorization
- household isolation
- unnecessary abstractions
- UX for a non-technical spouse
- tests

Classify CRITICAL/HIGH/MEDIUM/LOW.
Return findings and required fixes only.
```

---

# 10. Slice 2 — Financial Statement & Bill Ingestion

## Objective

Make financial-data import extremely easy by extracting and normalizing information from CSV files and PDF statements/bills for banks, credit cards, loans, and utilities.

The normal user experience is:

Upload CSV/PDF → identify source → extract → normalize → reconcile → auto-post high-confidence data → review exceptions → recalculate financial state.

## Product Focus

The application should prioritize **process quality, extraction accuracy, reconciliation, provenance, and exceptional UX** over institution API connectivity.

### Supported source categories

- Bank statements
- Credit-card statements
- Loan statements
- Utility bills/statements

### Supported file types

- CSV
- PDF

Image/scanned-document support may be retained as a controlled fallback where practical, but it is not the primary Slice 2 product path.

## Exclusions

No direct online institution connections, OAuth bank connections, provider sync, public LLM parsing, investment advice, money movement, or credit-card optimization.

## Initial Slice Prompt

```text
Implement Slice 2 — Financial Statement & Bill Ingestion.

The product priority is reliable, low-friction extraction of financial information from CSV files and PDF statements/bills.

Support these source categories:
- bank statements
- credit-card statements
- loan statements
- utility bills/statements

Support these primary file types:
- CSV
- PDF

The normal user experience must be:
Drop/import a file → identify source → process → reconcile → complete or guided review.

Build:
- FinancialDocument/Statement domain object
- encrypted MinIO storage
- checksum
- asynchronous processing
- source/category detection
- CSV parsing
- PDF text/table extraction
- OCR fallback when required
- transaction/financial-fact candidates
- normalization
- reconciliation
- confidence
- Review Queue
- posting
- FinancialSnapshot recalculation
- recurring-obligation extraction for utility bills
- user-friendly errors

For utilities, extract bill/obligation information rather than forcing utility bills into the Account model.

Processing must be asynchronous.
Raw source documents are immutable.
Reprocessing must be idempotent.
Duplicate data must never be silently created.
Ambiguous extraction/reconciliation must go to Review Queue.

Treat uploaded documents as untrusted input.
Do not use an LLM for primary extraction or reconciliation.
Do not implement online institution integrations in this slice.
Do not introduce separate microservices yet.
```

## Targeted Prompt 1 — Document/source domain and storage

```text
Implement the financial-document domain model, persistence, and storage foundation for Slice 2.

Support document/source types:
- BANK_STATEMENT
- CREDIT_CARD_STATEMENT
- LOAN_STATEMENT
- UTILITY_BILL
- FINANCIAL_CSV

Retain:
- original file
- checksum
- household ownership
- uploader
- upload time
- processing status
- source category
- institution/provider name when identifiable
- account/service relationship when identifiable
- statement/billing period
- processing version
- provenance

For utility documents, create or extend a recurring-obligation/bill model rather than treating the utility provider as a bank account.

Use MinIO for source files and PostgreSQL for structured metadata.

Do not implement parsing yet.
Add tests for authorization, checksum, duplicate source files, and state transitions.
```

## Targeted Prompt 2 — Import UX

```text
Implement the financial-data import UX.

The primary entry point should be simple:

[ Add Financial Data ]

Then allow:
- Drop a CSV or PDF
- Choose a file

The system should automatically identify whether the file appears to be:
- bank statement
- credit-card statement
- loan statement
- utility bill

Show human-readable processing progress:
- File received
- Identifying source
- Reading financial information
- Checking for duplicates
- Reconciling with existing data
- Updating your financial picture

Do not expose parser names, SQL errors, extraction libraries, queue names, or technical codes.

If identification is uncertain, ask one simple question:
"What type of document is this?"

Add Playwright coverage.
```

## Targeted Prompt 3 — CSV financial-data extraction

```text
Implement a provider-agnostic CSV financial-data parser framework.

Support CSV structures from:
- banks
- credit cards
- loans
- utilities

Common fields may include:
- date
- description
- amount
- debit
- credit
- balance
- transaction ID
- payment
- due date
- interest rate
- principal balance
- service period
- usage
- fees

Do not assume every CSV contains every field.

The parser should identify the source category and map fields into provider-neutral extraction contracts.

Produce extracted candidates rather than canonical transactions/bills.
Preserve original field values and source row numbers.
Reject ambiguous mappings rather than guessing silently.

Create fixtures and regression tests for each supported source category.
Do not use an LLM.
```

## Targeted Prompt 4 — PDF extraction by source category

```text
Implement PDF statement/bill extraction for:
- bank statements
- credit-card statements
- loan statements
- utility bills

Processing order:
1. file validation
2. text extraction
3. table extraction where applicable
4. OCR fallback only when required
5. source/category detection
6. metadata extraction
7. financial-fact/transaction/bill extraction

Extract, where present:
- institution/provider
- account/service identifier in redacted form
- statement/billing period
- opening/closing balance
- transactions
- payments
- fees
- interest rate
- minimum payment
- due date
- principal balance
- bill amount
- recurring charge
- utility usage

Run in the worker context with resource limits.
Treat document contents as untrusted data.
Do not fabricate missing values.
Low-confidence extraction becomes a review item.
```

## Targeted Prompt 5 — Normalized financial-fact model

```text
Implement the provider-neutral normalized financial-fact contract between parsers and the canonical financial model.

The contract must support at least:
- account observations
- transaction observations
- balance observations
- debt/loan observations
- payment observations
- bill/utility observations
- recurring-obligation observations

Every observation must retain provenance:
- source document
- page/row when available
- extracted field
- parser version
- confidence

Do not force every source into the Transaction model.
Utility bill facts and loan balance facts may feed different canonical domains.

Add serialization and contract tests.
```

## Targeted Prompt 6 — Reconciliation

```text
Extend reconciliation to support imported CSV/PDF observations across banks, credit cards, loans, and utilities.

For transactions, match using:
- source transaction ID when present
- account/service relationship
- amount
- date proximity
- merchant/provider normalization
- transaction direction

For bills/loan observations, additionally consider:
- billing/statement period
- due date
- statement balance
- prior balance
- payment amount

Return:
NEW
MATCHED
POSSIBLE_DUPLICATE
CONFLICT

Never silently duplicate data.
If balances or authoritative observations disagree, preserve both observations and create a reconciliation issue.
Add idempotency and overlap tests.
```

## Targeted Prompt 7 — Utility recurring obligations

```text
Implement extraction and normalization of recurring utility obligations.

Support utility bills containing:
- provider
- service period
- bill amount
- due date
- payment status when present
- recurring base charge
- taxes/fees
- usage when present

Create a recurring-obligation representation that can feed cash-flow forecasting.

Do not require a utility bill to have a traditional financial Account.

Calculate confidence for recurring patterns.
Do not predict future utility costs from one bill.
Use observed history when sufficient data exists.

Add fixtures for electricity, water, gas, telecommunications, and other utility-style statements where practical.
```

## Targeted Prompt 8 — Review Queue

```text
Extend the Review Queue for extraction and reconciliation exceptions.

Support:
- source identification uncertainty
- ambiguous field mapping
- ambiguous transaction
- possible duplicate
- balance mismatch
- overlapping statement period
- loan balance conflict
- utility bill ambiguity

Each item must explain:
What we found
Why we are unsure
What we need from you
What happens next

Keep the default experience non-technical.

User resolutions must be auditable and must not overwrite immutable source evidence.
```

## Targeted Prompt 9 — Posting and financial state

```text
Integrate high-confidence imported financial observations with the canonical financial model.

Rules:
- transactions post through the existing transaction pipeline
- loan observations update debt state through the existing debt model
- utility observations update recurring obligations/cash-flow inputs
- credit-card statement facts update the credit/debt model without confusing statement balance with revolving debt

Use transactional posting and idempotency.

After successful posting, recalculate FinancialSnapshot and relevant derived financial intelligence.

Review-required records must not silently affect canonical state.
Add failure and retry tests.
```

## Targeted Prompt 10 — Import summary and statement detail UX

```text
Complete the import experience.

After processing show a summary such as:

Statement processed
Bank account: Checking
Period: August 1–31
128 financial records found
126 imported
2 need your help

For a utility bill:

Utility bill processed
Service period: August
Bill: $182.40
Due: September 15
Recurring obligation detected: Yes

For a loan statement:

Loan statement processed
Balance: $24,300
Payment: $620
Interest rate: 6.4%

Use the actual extracted data; examples above are presentation patterns only.
```

## Targeted Prompt 11 — Integration

```text
Integrate the complete CSV/PDF ingestion path:

Upload → identify → extract → normalize → reconcile → review/auto-post → recalculate financial state.

Test each source category:
- bank
- credit card
- loan
- utility

Test:
- duplicate import
- overlapping statements
- parser failure
- low-confidence extraction
- balance mismatch
- partial review
- reprocessing
- household authorization

Do not add online institution integrations.
```

## Slice 2 Review Prompt

```text
Review Slice 2 for:
- extraction accuracy
- source-category correctness
- transaction integrity
- loan balance integrity
- utility obligation integrity
- duplicate protection
- immutable provenance
- parser security
- async reliability
- Review Queue correctness
- non-technical spouse UX
- regression fixture coverage

Do not add functionality.
Return ranked findings and required fixes.
```

---

# 11. Slice 3 — Financial Intelligence

## Objective

Turn clean transaction/account data into a continuously updated view of budget, cash flow, savings goals, emergency-fund health, debt, trends, and attention items.

## Initial Slice Prompt

```text
Implement Slice 3 — Financial Intelligence.

Build deterministic financial intelligence over the existing Slice 1 and Slice 2 data.

Implement:
- monthly budgets
- budget vs actual
- projected month-end spending
- recurring transaction detection
- cash flow
- short-term cash-flow forecast
- savings goals
- emergency-fund analysis
- debt analysis
- financial health status
- attention items
- historical trends
- metric explainability

Do not implement:
- public LLMs
- external research
- AI recommendations
- credit-card optimization
- advanced FI modeling
- online institution APIs

The AI must eventually consume these services, so keep all calculations structured, deterministic, versioned, and auditable.
```

## Targeted Prompt 1 — Budget engine

```text
Implement the deterministic budget engine.

For each category/period calculate:
- planned
- actual
- remaining
- variance
- variancePercent
- projectedMonthEnd

Do not infer budgets automatically from one month of history.
Support categories with zero activity and activity without a budget.
Add financial edge-case tests.
```

## Targeted Prompt 2 — Cash flow

```text
Implement historical and projected cash flow.

Calculate:
- starting cash
- income
- essential expenses
- discretionary spending
- goal funding
- projected ending cash
- monthly surplus

Use recurring patterns and explicit obligations where supported.
Do not produce false precision when data is weak.
Include confidence and assumptions.
```

## Targeted Prompt 3 — Recurring detection

```text
Implement deterministic recurring transaction detection.

Consider:
- merchant
- amount similarity
- frequency
- date spacing
- account
- transaction direction

Classify weekly, biweekly, monthly, quarterly, annual, irregular, unknown.
Store confidence.
A recurring observation must not automatically become a budget obligation.
```

## Targeted Prompt 4 — Goals/emergency fund

```text
Implement savings goals and emergency-fund analysis.

Support:
- emergency
- vacation
- entertainment
- project
- retirement
- custom

Calculate progress, required contribution, projected completion, and status.

Emergency fund targets must come from household policy.
Do not invent a universal target.
```

## Targeted Prompt 5 — Debt intelligence

```text
Implement deterministic debt intelligence.

Calculate:
- total debt
- revolving debt
- installment debt
- minimum payments
- scheduled payments
- interest rates
- weighted average rate where valid
- debt-to-income where supported

Distinguish statement balance, current balance, and revolving balance.
Do not yet prescribe a debt payoff strategy.
```

## Targeted Prompt 6 — Health/Attention

```text
Implement FinancialHealthStatus and AttentionItem.

Statuses:
HEALTHY
WATCH
AT_RISK
CRITICAL

Attention types:
- budget over
- cash-flow warning
- emergency fund low
- goal behind
- debt increase
- data stale
- recurring expense change

The engine should identify conditions, not make recommendations.
```

## Targeted Prompt 7 — Dashboard

```text
Implement the Slice 3 Financial Intelligence dashboard.

The spouse must understand within 30 seconds:
- how the household is doing
- where money is going
- whether spending is on plan
- savings progress
- debt condition
- what needs attention

Use progressive disclosure for details and calculations.
```

## Targeted Prompt 8 — Trends/explainability

```text
Implement 3/6/12-month historical trends and explainability.

Every key metric should expose:
- value
- as-of date
- calculation version
- source inputs
- assumptions

Historical results must remain reproducible after future calculation-rule changes.
```

## Targeted Prompt 9 — Integration

```text
Integrate:
transactions → budgets → recurring patterns → cash flow → goals → emergency fund → debt → health → attention → dashboard.

Test transaction changes propagating into derived state.
Add healthy, overspending, low-emergency, cash-deficit, goal-behind, and debt-increase scenarios.
```

## Slice 3 Review Prompt

```text
Review Slice 3 for:
- money correctness
- date boundaries
- refunds/transfers
- credit-card balances
- historical reproducibility
- calculation versions
- derived state consistency
- rule duplication
- UX clarity
- test coverage

Do not add functionality.
Return ranked findings and fixes.
```

---

# 12. Slice 4 — AI Financial Advisor & Conversational Budget Planning

## Objective

Turn Slices 1–3 into a conversational planning assistant that understands information, diagnosis, planning, and scenarios.

## Critical capabilities

The AI must understand:

### Information

- How are we doing financially?
- What changed this month?
- Where are we spending too much?
- Are our goals on track?

### Budget creation

- Help me create an initial budget.
- Help us create our first budget.
- What should our monthly budget look like?

### Budget diagnosis

- Why am I always over budget?
- Why do I keep going over budget?
- Is our budget unrealistic?
- What's causing us to go over budget?

### Budget revision

- Help me revise the budget for next month with these known activities.
- We have a car repair and a trip next month. Help us adjust the budget.
- What should next month's budget look like?

### Scenarios

- Can we afford a $4,000 kitchen project?
- What if it costs $6,000?
- What happens if we reduce spending by $300?

## Initial Slice Prompt

```text
Implement Slice 4 — AI Financial Advisor and Conversational Budget Planning.

Build a tool-driven AI layer over the deterministic financial intelligence in Slices 1–3.

Support four modes:
INFORMATION
DIAGNOSIS
PLANNING
SCENARIO

Support workflows:
FINANCIAL_HEALTH
BUDGET_STATUS
BUDGET_CREATE
BUDGET_DIAGNOSE
BUDGET_REVISE
BUDGET_SCENARIO
CASH_FLOW
GOAL_STATUS
DEBT_STATUS
AFFORDABILITY
GENERAL_FINANCIAL_QUESTION

The AI must:
- retrieve current financial context automatically;
- use typed tools;
- distinguish fact/calculation/assumption/analysis/proposal/approved decision;
- maintain conversational context;
- never directly query the database;
- never invent financial values;
- never modify financial state without explicit approval;
- protect restricted data through the privacy boundary.

Initial tools:
get_financial_snapshot
get_cash_flow
get_current_budget
get_budget_status
get_historical_budget_performance
get_goal_status
get_debt_summary
get_attention_items
get_recurring_financial_items
simulate_purchase
simulate_budget_change
create_initial_budget
analyze_budget_variance
plan_next_month_budget

Implement provider-neutral LLM abstraction.
Implement privacy gateway.
Implement graceful LLM failure.
Implement response grounding.

Do not implement research/validated recommendations yet.
Do not implement online institution integrations.
Do not implement automated money movement.
Do not create a generic autonomous-agent framework.
```

## Targeted Prompt 1 — AI/domain workflows

```text
Implement AI conversation and workflow contracts.

Create:
- AdvisorConversation
- AdvisorMessage
- AdvisorWorkflow
- WorkflowState
- AIRequest
- AIResponse
- ToolExecution

Workflow types:
INFORMATION
DIAGNOSIS
PLANNING
SCENARIO

Persist structured workflow state separately from conversation history.
```

## Targeted Prompt 2 — Tool contracts

```text
Implement typed contracts for:
- get_financial_snapshot
- get_cash_flow
- get_current_budget
- get_budget_status
- get_historical_budget_performance
- get_goal_status
- get_debt_summary
- get_attention_items
- get_recurring_financial_items
- simulate_purchase
- simulate_budget_change
- create_initial_budget
- analyze_budget_variance
- plan_next_month_budget

Tools must delegate to existing financial domain services.
No generic query tool.
Add schema and authorization tests.
```

## Targeted Prompt 3 — Intent/workflow classifier

```text
Implement controlled intent classification for:
FINANCIAL_HEALTH
BUDGET_STATUS
BUDGET_CREATE
BUDGET_DIAGNOSE
BUDGET_REVISE
BUDGET_SCENARIO
CASH_FLOW
GOAL_STATUS
DEBT_STATUS
AFFORDABILITY
GENERAL_FINANCIAL_QUESTION

Map each to INFORMATION, DIAGNOSIS, PLANNING, or SCENARIO.
Do not use brittle keyword-only matching.
Support future model-based classification but constrain outputs to enums.
```

## Targeted Prompt 4 — Context Builder

```text
Implement the Financial Context Builder.

Map user intent to only the required financial tools.

For example:
"Why am I always over budget?"
→ current budget + historical performance + recurring items + variance analysis.

"Help me create an initial budget."
→ financial snapshot + cash flow + historical spending + debt + goals + recurring items + policy.

"Help me revise next month's budget."
→ current budget + next-month cash flow + recurring obligations + goals + known activities.

Do not retrieve all transactions by default.
Do not use conversation memory as authoritative financial state.
```

## Targeted Prompt 5 — Budget planning integration

```text
Implement:
create_initial_budget()
analyze_budget_variance()
plan_next_month_budget()
simulate_budget_change()

For initial budget creation, distinguish observed spending from proposed allocations.

For budget diagnosis, identify:
- unrealistic budget
- recurring overrun
- missing annual expense
- missing recurring expense
- irregular expense
- categorization problem
- timing issue

For next-month planning, extract known activities from conversation and calculate:
- proposed budget
- changes
- projected cash
- goal impact
- emergency-fund impact
- assumptions
- confidence
```

## Targeted Prompt 6 — Conversation planning state

```text
Implement structured conversational planning state.

A user must be able to say:
"Help me revise next month's budget."
then:
"We have a $1,200 car repair, a $900 birthday, and a $1,500 trip."
then:
"I don't want to reduce vacation savings."

Retain:
- planning period
- known activities
- constraints
- assumptions
- proposed changes
- pending questions

Always recalculate using current financial state.
```

## Targeted Prompt 7 — LLM adapter

```text
Implement a provider-neutral LLM adapter.

Interface:
LLMProvider.generateResponse(request)

Initial provider: one public LLM.
Future providers:
- other public providers
- local Ollama-compatible provider

Provider SDKs must remain inside the adapter.
The provider receives only sanitized context.
```

## Targeted Prompt 8 — Privacy Gateway

```text
Implement the external LLM privacy gateway.

Restricted:
- account numbers
- routing numbers
- card numbers
- credentials
- government identifiers
- raw statements
- authentication tokens

Default deny external egress.
Allowlist destinations.
Minimize context.
Add automated tests proving restricted data cannot leave the self-hosted environment.
```

## Targeted Prompt 9 — AI orchestrator

```text
Implement the AI Orchestrator.

Flow:
user message → intent → workflow state → tool plan → authorization → tools → context → privacy gateway → LLM → response validation → user.

No arbitrary SQL.
No arbitrary URLs.
No shell.
No unrestricted filesystem access.
```

## Targeted Prompt 10 — Advisor UX

```text
Implement the conversational advisor UX.

Suggested prompts:
- How are we doing financially?
- Help me create an initial budget.
- Why am I always over budget?
- Help me revise the budget for next month.
- What changed this month?
- Can we afford a $4,000 project?

For planning workflows show:
Proposed Plan
Why
Impact
Assumptions
Review
Approve

Do not show raw tool names or technical errors.
```

## Targeted Prompt 11 — Grounding

```text
Implement response grounding.

Validate material numeric and factual claims against tool outputs.
Detect:
- unsupported numbers
- unsupported account claims
- contradictions
- fabricated research
- assumptions stated as facts

If a response cannot be safely grounded, return a structured fallback using authoritative tool results.
```

## Targeted Prompt 12 — Approval/persistence

```text
Implement explicit approval for AI-generated budget changes.

Proposal → financial validation → user review → optional user changes → recalculation → explicit approval → new budget version.

The LLM cannot directly persist changes.

Record:
- prior version
- new version
- approved changes
- approving user
- timestamp
- conversation
- FinancialSnapshot version
```

## Targeted Prompt 13 — Failure handling

```text
Implement graceful handling of:
- LLM unavailable
- LLM timeout
- rate limit
- malformed response
- tool failure
- privacy rejection
- stale snapshot
- financial-engine unavailable

Financial data must remain usable even if the AI is unavailable.
Never replace missing calculations with guesses.
```

## Targeted Prompt 14 — Conversation continuity

```text
Implement conversational continuity.

Support follow-ups such as:
"What if it's $6,000?"
following:
"Can we afford a $4,000 kitchen project?"

Conversation state supplies subject context.
Current structured financial data remains authoritative.
```

## Targeted Prompt 15 — Audit

```text
Implement AI interaction audit metadata.

Record:
- conversation
- user
- workflow
- intent
- tools
- tool versions
- snapshot version
- provider/model
- validation status
- approval status
- correlation ID

Do not log restricted financial payloads.
```

## Targeted Prompt 16 — E2E workflows

```text
Create end-to-end tests for:

1. How are we doing financially?
2. Help me create an initial budget.
3. Why am I always over budget?
4. Help me revise next month's budget with known activities.
5. I don't want to reduce vacation savings.
6. Can we afford a $4,000 project?
7. What if it is $6,000?
8. LLM unavailable.
9. Privacy gateway rejects restricted data.

Verify correct tools, current financial state, grounding, approval boundaries, and UX.
```

## Slice 4 Review Prompt

```text
Review Slice 4 for:
- AI tool safety
- unrestricted data access
- privacy leakage
- stale-context problems
- business logic in prompts
- financial calculation duplication
- planning-state integrity
- approval bypasses
- provider coupling
- conversational UX
- hallucination grounding

Do not add functionality.
Return ranked findings and required fixes.
```

---

# 13. Slice 5 — Research, Recommendation & Independent Validation

## Objective

Turn the AI Advisor from a planning assistant into a **researched, challengeable recommendation engine**.

This is where the original product requirement of:

> professional, researched-based guidance with unbiased validation on all recommendations

becomes a formal architecture capability.

## Design principle

Research does not make the recommendation.

The research service establishes current facts and evidence.

The financial engine calculates household impact.

The recommendation engine creates candidate actions.

The validation engine challenges them.

The final AI response explains the result.

## Initial Slice Prompt

```text
Implement Slice 5 — Research, Recommendation, and Independent Validation.

Build a governed recommendation pipeline:

user intent
→ current financial context
→ deterministic financial scenarios
→ current research
→ candidate recommendation
→ independent validation
→ final recommendation
→ decision journal

Every material recommendation must include:
- recommendation
- alternatives
- financial impact
- assumptions
- evidence
- confidence
- validation result
- approval requirement

Research must distinguish source quality:
Tier 1: government/regulator/official sources
Tier 2: primary provider/issuer sources
Tier 3: independent institutional research
Tier 4: financial media/community context

Current product-specific facts must prefer primary sources.

Do not allow the LLM to claim research was performed unless the research service actually retrieved it.

Do not let the research agent directly make household recommendations.

Do not yet implement automated investment trading or money movement.
```

## Targeted Prompt 1 — Evidence domain

```text
Implement EvidenceSource, ResearchClaim, and EvidenceBundle.

Store:
- source URI
- source type
- retrieval timestamp
- freshness
- publisher
- claim
- source tier
- relevant excerpt/summary where legally appropriate
- source checksum/version when useful

Every research-backed material claim must reference evidence.
```

## Targeted Prompt 2 — Research service

```text
Implement a research service that retrieves current external information.

Support source allowlists and scoring.

Prefer primary sources.

Return structured evidence, not a free-form recommendation.

If current information cannot be verified, mark the fact as unavailable or stale.
```

## Targeted Prompt 3 — Recommendation model

```text
Implement Recommendation and RecommendationOption models.

Include:
- recommendation ID
- intent
- financial snapshot version
- policy version
- primary recommendation
- alternatives
- impact
- assumptions
- evidence
- confidence
- approval requirements
- validation status
- decision state
```

## Targeted Prompt 4 — Recommendation engine

```text
Implement a deterministic recommendation orchestration layer.

Combine:
- household policy
- current financial state
- financial scenarios
- validated research

Generate candidate options rather than a single unexplained answer.

The LLM may help articulate candidates but cannot bypass financial calculations or evidence requirements.
```

## Targeted Prompt 5 — Independent validator

```text
Implement the Recommendation Validation Engine.

Challenge each material recommendation for:
- mathematical correctness
- household policy conflicts
- unsupported assumptions
- missing alternatives
- downside risk
- evidence freshness
- potential conflicts/bias

Return PASS/WARN/FAIL plus reasons.
```

## Targeted Prompt 6 — Adversarial review

```text
Add an independent challenge pass.

Ask:
- What could make this recommendation wrong?
- What alternative might be better?
- Which assumption matters most?
- What information is missing?
- Is the recommendation consistent with household policy?

Keep adversarial output separate from primary recommendation generation.
```

## Targeted Prompt 7 — Recommendation UX

```text
Create the recommendation card.

Show:
Recommendation
Why
Financial impact
Alternatives
Evidence
Confidence
What could make this wrong

Progressively disclose calculations and source details.
```

## Targeted Prompt 8 — Decision journal

```text
Implement DecisionJournalEntry.

Capture:
- question
- recommendation
- alternatives
- evidence
- validation
- household state
- decision
- approval
- later outcome if known

Support later retrospective analysis.
```

## Targeted Prompt 9 — Scenario tests

```text
Create recommendation scenarios covering:
- emergency fund funding
- budget allocation
- debt consideration
- vacation affordability
- unexpected expense
- unexpected money

Verify evidence, validation, confidence, and alternatives.
```

## Slice 5 Review Prompt

```text
Review Slice 5 for:
- evidence provenance
- stale research
- unsupported claims
- recommendation bias
- validator independence
- policy conflicts
- missing alternatives
- approval boundaries
- auditability

Do not add functionality.
Return ranked findings.
```

---

# 14. Slice 6 — Advanced Import Intelligence

## Objective

Improve the CSV/PDF financial-data ingestion experience without introducing online institution connectivity. The goal is to make manual import increasingly automatic, accurate, explainable, and resilient across banks, credit cards, loans, and utilities.

## Initial Slice Prompt

```text
Implement Slice 6 — Advanced Import Intelligence.

Do not implement online institution integrations. Their timing is TBD.

Improve the existing CSV/PDF import pipeline for:
- banks
- credit cards
- loans
- utilities

Focus on:
- source/template learning
- improved institution/source identification
- parser confidence
- reusable CSV mappings
- PDF extraction accuracy
- better reconciliation
- recurring obligation detection
- statement-to-statement comparison
- balance continuity
- user-approved learned corrections
- explainable import results

Preserve the canonical flow:
CSV/PDF → raw source → extract → normalize → reconcile → validate → canonical financial state → FinancialSnapshot.

The system should become easier to use over time without requiring online bank connections.
```

## Targeted Prompt 1 — Source/template profiles

```text
Create versioned source/template profiles for recurring CSV/PDF layouts.

A profile may contain:
- source category
- institution/provider name
- detected columns
- date format
- amount format
- common labels
- document-layout hints
- parser strategy
- confidence rules

Profiles must be reusable but never trusted blindly.
Validate every import against the actual document.
```

## Targeted Prompt 2 — User-approved learning

```text
Implement learning from user-approved Review Queue corrections.

Examples:
- field mapping correction
- transaction category correction
- merchant normalization
- document source type
- recurring obligation classification

Corrections must be:
- household-scoped where appropriate
- versioned
- reversible
- explicitly approved for learning

Never silently train on a correction.
```

## Targeted Prompt 3 — Statement comparison

```text
Implement statement-to-statement comparison.

For sequential statements identify:
- opening/closing balance continuity
- missing periods
- duplicated periods
- large balance changes
- new recurring charges
- removed recurring charges
- changed loan payments
- changed utility charges

Surface discrepancies through Review Queue or Financial Attention items.
Do not invent explanations.
```

## Targeted Prompt 4 — Advanced recurring financial facts

```text
Improve recurring detection across:
- bank deposits
- bills
- utility charges
- loan payments
- credit-card payments
- subscriptions/recurring household expenses

Require multiple observations before marking a recurring pattern with high confidence.
Keep recurring observations separate from user-approved budget obligations.
```

## Targeted Prompt 5 — Import quality scoring

```text
Create an import-quality score that summarizes:
- extraction completeness
- field confidence
- reconciliation confidence
- balance consistency
- duplicate risk
- unresolved exceptions

Show a plain-language result to the user, not a technical parser score.

Example:
\"Import quality: Excellent — all records were reconciled and no issues were found.\"
```

## Targeted Prompt 6 — Import UX refinement

```text
Refine the import UX using actual household testing.

Optimize for:
- fewer steps
- fewer questions
- understandable progress
- easy correction
- obvious completion state
- easy reprocessing

The user should not need to understand the difference between a bank, credit-card, loan, or utility parser.
```

## Targeted Prompt 7 — Regression corpus

```text
Build a regression corpus of anonymized CSV/PDF examples across:
- checking
- savings
- credit card
- auto/personal/mortgage-style loans
- electric
- gas
- water
- telecommunications

Track parser regressions and extraction accuracy over time.
```

## Targeted Prompt 8 — Slice 6 integration

```text
Integrate advanced import intelligence into the existing ingestion pipeline.

Verify that improvements never bypass:
- immutable source storage
- provenance
- reconciliation
- Review Queue
- household authorization
- FinancialSnapshot versioning

Do not add online institution connectivity.
```

## Slice 6 Review Prompt

```text
Review Slice 6 for:
- extraction accuracy
- false confidence
- learned-correction safety
- provenance
- reconciliation
- statement continuity
- utility obligation accuracy
- loan balance accuracy
- privacy
- UX simplicity

Confirm that the application remains fully useful with CSV/PDF imports alone.
Do not add online institution integrations.
```

---

# 15. Slice 7 — Advanced Financial Optimization

## Objective

Add the highest-value decision capabilities:

- surprise expenses;
- unplanned influx of money;
- credit-card optimization;
- financial independence planning;
- richer scenarios.

## Initial Slice Prompt

```text
Implement Slice 7 — Advanced Financial Optimization.

Build four governed engines:
1. Surprise Expense Engine
2. Windfall Allocation Engine
3. Credit Card Optimization Engine
4. Financial Independence Scenario Engine

All must operate on current structured financial state.

All recommendations must remain proposals until explicitly approved.

Credit-card product facts must come from current evidence/issuer terms.

FI projections must clearly expose assumptions and uncertainty.

Do not implement automated financial transfers or trading.
```

## Targeted Prompt 1 — Surprise expense

```text
Implement simulate_expense().

Evaluate:
- available cash
- emergency fund
- upcoming obligations
- debt cost
- goals
- credit options

Return funding alternatives and downstream impacts.
Do not prescribe without recommendation validation.
```

## Targeted Prompt 2 — Windfall

```text
Implement simulate_windfall().

Support:
- bonus
- tax refund
- inheritance
- gift
- side income
- settlement
- asset sale proceeds

Consider:
- tax uncertainty
- emergency fund
- high-interest debt
- goals
- retirement
- investments
- discretionary use

Return multiple allocations with trade-offs.
```

## Targeted Prompt 3 — Credit cards

```text
Implement CreditCardProfile, RewardProgram, benefits, annual fee, and transaction optimization.

Use current primary-source terms.

For a transaction determine:
- eligible rewards
- reward value
- annual fee implications
- relevant benefits
- available credit constraints
- purchase protections where supported

Never recommend based on stale product terms.
```

## Targeted Prompt 4 — Financial independence

```text
Implement FI projections and scenario analysis.

Inputs:
- current age
- assets
- contributions
- spending
- inflation assumption
- return assumptions
- retirement age
- other household assumptions

Support deterministic projections and Monte Carlo scenarios.

Clearly distinguish assumptions from observed data.
```

## Targeted Prompt 5 — Scenario center

```text
Implement a unified scenario center for:
- purchase
- budget change
- windfall
- surprise expense
- retirement timing
- savings changes

Each scenario should preserve inputs, outputs, assumptions, and version.
```

## Targeted Prompt 6 — AI tools

```text
Add typed advisor tools:
- simulate_expense
- simulate_windfall
- get_credit_card_options
- project_financial_independence
- get_scenario

Do not allow the AI to directly calculate these results.
```

## Targeted Prompt 7 — UX

```text
Create simple workflows for:

"We have an unexpected $3,000 expense. What should we do?"

"We received a $10,000 bonus. What should we do with it?"

"Which card should I use for this purchase?"

"When can we retire?"

Show alternatives and impact without overwhelming the user.
```

## Slice 7 Review Prompt

```text
Review financial optimization for:
- assumptions
- stale data
- current product terms
- numerical correctness
- risk communication
- goal trade-offs
- spouse approval
- model uncertainty
- recommendation grounding

Do not add functionality.
```

---

# 16. Slice 8 — Privacy, Security, Resilience & UX Hardening

## Objective

Turn a useful prototype into a trustworthy private household application.

## Initial Slice Prompt

```text
Implement Slice 8 — Security, Privacy, Resilience, and UX Hardening.

Harden:
- authentication
- household authorization
- private-member data
- database encryption
- document encryption
- secrets management
- outbound network policy
- LLM privacy gateway
- file upload security
- prompt injection defenses
- audit logging
- backups
- restore testing
- error recovery
- UX accessibility

The system must remain useful when:
- public LLM is unavailable;
- research provider is unavailable;
- online institution provider is unavailable;
- statement parsing fails.
```

## Targeted Prompt 1 — Threat model

```text
Create and implement controls for threats including:
- public LLM data leakage
- malicious statement upload
- prompt injection in documents
- unauthorized spouse access
- credential compromise
- arbitrary external egress
- financial data corruption
- insecure logs
- backup compromise
```

## Targeted Prompt 2 — Privacy tests

```text
Create automated outbound-data scanning tests.

Verify no LLM request contains:
- account number
- routing number
- card number
- credentials
- government identifiers
- raw statement content

Also verify all external traffic passes through allowlisted privacy infrastructure.
```

## Targeted Prompt 3 — Authorization

```text
Test household isolation and spouse/member permissions.

A member must never retrieve another household's financial data.

Private individual data must remain private unless explicitly shared.
```

## Targeted Prompt 4 — File security

```text
Harden statement/document processing.

Add:
- file type validation
- size limits
- decompression limits
- parser timeouts
- sandboxing/resource limits
- malware scanning where available
- dangerous-content isolation
```

## Targeted Prompt 5 — Backup/restore

```text
Implement encrypted backups of PostgreSQL and MinIO metadata/documents.

Create automated restore tests.

Definition of done is successful restore, not successful backup creation.
```

## Targeted Prompt 6 — UX error audit

```text
Perform a complete UX error audit.

Every recoverable error must provide:
What happened
Why
What to do
Primary Fix action
What happens next

Remove technical jargon from normal UX.
```

## Targeted Prompt 7 — Accessibility

```text
Audit and improve accessibility:
- keyboard navigation
- contrast
- readable type
- responsive layout
- focus states
- accessible forms
- screen-reader labels
- clear status messaging
```

## Slice 8 Review Prompt

```text
Perform a security/privacy/UX readiness review.

Do not add functionality.

Classify risks:
CRITICAL/HIGH/MEDIUM/LOW.

Determine whether the application is safe for private household use.
```

---

# 17. Slice 9 — Productionization & Operational Readiness

## Objective

Make the self-hosted product reproducible, observable, backup-safe, and easy to operate.

## Initial Slice Prompt

```text
Implement Slice 9 — Productionization.

Build:
- production Docker Compose configuration
- environment separation
- secrets management
- database migrations
- health checks
- readiness checks
- observability
- structured diagnostics
- backup automation
- restore tooling
- upgrade procedure
- rollback procedure
- operational runbooks
- data export/import

The product should be reproducible on a new Docker host using documented steps.
```

## Targeted Prompt 1 — Docker deployment

```text
Create development and production Docker Compose configurations.

Use separate networks for:
frontend
application
data
restricted-egress

Only required containers should have external network access.
```

## Targeted Prompt 2 — Observability

```text
Implement metrics, logs, and traces.

Track:
- ingestion success
- reconciliation confidence
- review queue size
- FinancialSnapshot freshness
- AI failures
- research freshness
- recommendation validation
- import pipeline health

Never log restricted financial payloads.
```

## Targeted Prompt 3 — Diagnostics

```text
Create an administrator-only diagnostics view.

Expose correlation ID, workflow status, error category, and processing history without exposing unnecessary financial content.
```

## Targeted Prompt 4 — Upgrade/rollback

```text
Create documented procedures for:
- schema migration
- application upgrade
- rollback
- database restore
- MinIO restore
- reprocessing statements
- replaying failed jobs
```

## Targeted Prompt 5 — Disaster recovery

```text
Test:
- database corruption
- MinIO failure
- Redis loss
- worker crash
- API crash
- LLM provider outage
- online institution provider outage

Document recovery behavior.
```

## Slice 9 Review Prompt

```text
Review production readiness for:
- reproducible deployment
- backups
- restore
- migration safety
- observability
- external egress
- operational documentation
- recovery testing

Do not add functionality.
```

---

# 18. AI Persona Selection Feature

## Objective

Allow the household user to choose an **AI advisor persona** that influences the style, emphasis, questions, and framework used when presenting advice.

### Critical design rule

These personas should be treated as **named advisory styles/frameworks**, not as impersonations of real people or representations that the named person/organization endorses the application.

The system should never imply:

- the real person is providing the answer;
- the real organization reviewed the household finances;
- the person endorsed the product;
- proprietary/private prompts from those individuals or organizations are being used unless legitimately licensed or sourced.

Use persona labels such as:

> "Kitces-inspired retirement planning framework"

or, if the user chooses the exact supplied label:

> "Michael Kitces — retirement planning emphasis"

with an unobtrusive notice:

> "This is an AI advisory style inspired by publicly described areas of expertise. It is not the real person or organization."

## Persona model

Create:

```text
AdvisorPersona
- id
- name
- shortDescription
- planningFocus
- communicationStyle
- questionStyle
- analysisPriorities
- cautionAreas
- defaultWorkflows
- systemBehaviorRules
- version
- enabled
```

The persona must never change:

- financial facts;
- calculations;
- household policy;
- validation requirements;
- privacy policy;
- evidence requirements.

Persona changes **presentation and analytical emphasis**, not financial truth.

---

# 19. Persona Catalog

## Persona 1 — Michael Kitces / Kitces-style Planning

**Focus:** retirement planning, financial frameworks, client discovery, tax scenario modeling, financial life planning.

**Best for:**

- retirement timing;
- withdrawal strategy analysis;
- financial independence;
- tax-sensitive scenarios;
- life-planning trade-offs;
- asking better discovery questions.

**Communication characteristics:**

- structured;
- analytical;
- framework-oriented;
- explicit about assumptions;
- strong emphasis on long-term planning and trade-offs.

**Prompt behavior:**

- emphasize planning horizon;
- identify missing retirement assumptions;
- examine tax and sequence-of-events implications where relevant;
- distinguish financial planning framework from a specific product recommendation.

---

## Persona 2 — Ric Edelman / Edelman-style Modern Wealth Planning

**Focus:** wealth management technology, digital assets, longevity, estate planning, multi-generational wealth, modern portfolio concepts.

**Best for:**

- longevity planning;
- technology-assisted financial planning;
- estate/legacy discussions;
- digital asset education;
- multi-generational planning.

**Communication characteristics:**

- forward-looking;
- practical;
- technology-aware;
- emphasizes modernization and long-term resilience.

**Prompt behavior:**

- consider longevity risk;
- consider technology/digital-asset implications when relevant;
- distinguish education from individualized regulated investment advice;
- identify estate/legacy considerations when material.

---

## Persona 3 — Ron Carson / Carson-style Family Wealth Planning

**Focus:** high-net-worth family planning, business transition, cash-flow roadmaps, family governance, multi-step planning.

**Best for:**

- complex household planning;
- family governance;
- business-owner scenarios;
- transition planning;
- multi-step cash-flow strategies.

**Communication characteristics:**

- action-oriented;
- roadmap-driven;
- focused on implementation;
- emphasizes family-level decision making.

**Prompt behavior:**

- organize decisions into phases;
- identify responsibilities and dependencies;
- surface family governance considerations;
- create concrete next actions.

---

## Persona 4 — Wealth Enhancement Group / WEG-style Integrated Planning

**Focus:** integrated tax, estate, personal financial modeling, retirement plans, debt-paydown scenarios.

**Best for:**

- tax-sensitive planning;
- debt analysis;
- retirement contributions;
- integrated household modeling;
- multi-account planning.

**Communication characteristics:**

- integrated;
- practical;
- scenario-driven;
- emphasizes interaction between financial decisions.

**Prompt behavior:**

- look for interactions between taxes, debt, cash flow, retirement, and goals;
- present scenario comparisons;
- flag areas requiring professional tax/legal review.

---

## Persona 5 — Capital Group Advisor Practice / PracticeLab-style Family Wealth Planning

**Focus:** multi-generational wealth, tax trends, estate workflows, education funding, structured planning workflows.

**Best for:**

- education savings;
- multi-generational planning;
- estate workflow discussions;
- structured document analysis;
- long-term family wealth.

**Communication characteristics:**

- structured;
- family-oriented;
- workflow-oriented;
- emphasizes planning across generations.

**Prompt behavior:**

- consider education and legacy objectives;
- organize complex planning into structured workflows;
- surface estate/tax dependencies;
- avoid presenting assumptions as professional legal/tax determinations.

---

# 20. Persona Architecture

The correct architecture is:

```text
User Question
      ↓
Current Household Financial Context
      ↓
Financial Engine
      ↓
Research / Evidence
      ↓
Recommendation / Validation
      ↓
Persona Adapter
      ↓
LLM
      ↓
Grounded Response
```

NOT:

```text
Persona
   ↓
Financial Calculation
```

The persona is downstream of the financial truth.

## Persona invariants

Regardless of persona:

- financial numbers remain identical;
- recommendations remain constrained by household policy;
- evidence standards remain identical;
- validation standards remain identical;
- privacy standards remain identical;
- regulatory boundaries remain identical.

Only emphasis, tone, framing, and planning questions may change.

---

# 21. Persona Selection UX

The user should be able to select:

> **How would you like your advisor to approach this?**

Options:

### Retirement & Planning
Kitces-style

### Modern Wealth & Longevity
Edelman-style

### Family Wealth & Roadmaps
Carson-style

### Integrated Tax & Planning
WEG-style

### Multi-Generational Wealth
Capital Group Practice-style

### Balanced Household Advisor
Default neutral persona

The neutral persona should be the default.

The interface should make clear that the persona changes **how advice is framed**, not the underlying financial facts or validation rules.

---

# 22. Persona Prompt Contract

Every persona should be represented as structured configuration rather than hard-coded prompt fragments distributed across the application.

Example:

```json
{
  "id": "retirement-planning",
  "displayName": "Retirement & Planning",
  "style": "analytical, framework-oriented",
  "focus": [
    "retirement planning",
    "financial independence",
    "tax-aware scenarios",
    "life planning"
  ],
  "questionStyle": [
    "surface missing assumptions",
    "identify planning horizon",
    "compare scenarios"
  ],
  "invariants": {
    "requiresEvidence": true,
    "requiresValidation": true,
    "preserveFinancialTruth": true
  }
}
```

The system prompt should combine:

```text
base financial advisor contract
+
household financial policy
+
current workflow
+
structured financial context
+
selected persona configuration
```

Never put raw household financial data into a long-lived persona definition.

---

# 23. Persona Targeted Prompts

## Prompt — Persona domain

```text
Implement AdvisorPersona and PersonaConfiguration.

Support:
- persona selection
- household/member default persona
- conversation-level persona override
- persona versioning
- enabled/disabled state

Persona configuration may affect presentation and analytical emphasis only.
It may not alter financial calculations, evidence standards, privacy policy, or validation requirements.
```

## Prompt — Persona registry

```text
Implement a versioned persona registry containing the five requested advisory styles plus a neutral default.

Each persona must define:
- focus areas
- communication style
- preferred planning questions
- analytical emphasis
- caution areas
- workflow preferences

Do not claim these are official prompts, endorsements, or direct outputs of the named people/organizations.
Use public-domain/generalized descriptions only unless separately licensed.
```

## Prompt — Persona selection UX

```text
Implement a simple persona selector.

Show:
- name
- short description
- best for

Explain:
"Your selected approach changes how the advisor frames its analysis. The underlying financial data, calculations, evidence standards, and validation rules stay the same."

Allow:
- household default
- per-conversation override
```

## Prompt — Persona orchestration

```text
Add persona configuration to AI context assembly.

The selected persona must be applied after authoritative financial context is created and before natural-language generation.

Do not allow persona prompts to override:
- financial facts
- calculations
- household policies
- safety constraints
- privacy policy
- validation requirements.
```

## Prompt — Persona comparison test

```text
Given the same household financial state and question, generate responses using all five personas plus the neutral persona.

Verify:
- identical financial facts
- identical deterministic calculation results
- equivalent validation outcomes
- different but appropriate framing
- no persona claims unsupported expertise or endorsement.
```

---

# 24. Cross-Slice Prompt Generation Framework

For any future vertical slice that has not yet been designed, use the following prompt sequence.

## A. Slice Scoping Prompt

```text
Define a new vertical slice for the AI Financial Advisor.

Objective:
<business outcome>

Primary user journey:
<user journey>

Inputs:
<existing domain/data>

Outputs:
<new capability>

Architecture boundaries:
<modules/services that may change>

Non-goals:
<explicit exclusions>

UX requirements:
<simple user experience>

Financial invariants:
<rules that must not be broken>

Privacy/security constraints:
<data handling and permissions>

AI constraints:
<what the LLM may and may not do>

Definition of done:
<observable acceptance criteria>

Testing:
<unit/integration/E2E/privacy/financial scenarios>

Before coding inspect the repository and existing architecture.
Do not introduce unrelated abstractions or microservices.
Return implementation risks before coding.
```

## B. Targeted Domain Prompt

```text
Implement the domain model and invariants for this slice.

Do not build UI or external integrations yet.

Create:
<entities/value objects/services>

Preserve existing domain boundaries.
Add deterministic tests first where financial behavior is involved.
```

## C. Targeted Calculation/Business Logic Prompt

```text
Implement the deterministic business/financial engine for this slice.

Use existing Money and financial-domain primitives.
Do not duplicate business rules in controllers, UI, or AI tools.

Add scenario fixtures and edge-case tests.
```

## D. Targeted API/Contract Prompt

```text
Implement the typed API and application contracts for this slice.

Use existing domain services.
Validate schemas.
Enforce household/member authorization.
Do not expose unnecessary internal identifiers.
```

## E. Targeted UX Prompt

```text
Implement the user experience for this slice.

Primary audience includes a non-technical spouse.

Hide technical details.
Use progressive disclosure.
Every recoverable error gets:
- what happened
- why
- what to do
- Fix action
- expected next step

Do not duplicate domain calculations in the UI.
Add Playwright coverage.
```

## F. Integration Prompt

```text
Integrate the completed slice end to end.

Use real domain services and real persistence.
Remove production-path mocks.
Verify authorization, error recovery, idempotency, and financial correctness.
Add E2E tests.
```

## G. Review Prompt

```text
Review this slice without adding functionality.

Evaluate:
- architecture
- domain boundaries
- financial correctness
- data integrity
- security
- privacy
- AI safety
- UX
- accessibility
- observability
- test coverage

Classify CRITICAL/HIGH/MEDIUM/LOW.
Return findings and required fixes.
```

---

# 25. Recommended Development Cadence

For a typical slice:

```text
Day / Iteration 1
Slice definition
↓
Domain contracts
↓
Tests

Iteration 2
Business engine
↓
API

Iteration 3
UX
↓
Integration

Iteration 4
Real-world scenario testing
↓
Architecture/UX/security review
↓
Targeted fixes
```

Do not measure progress by number of generated files.

Measure by completed user journeys.

---

# 26. Golden User Journeys

The product should eventually pass these demonstrations.

## Journey 1 — New household

```text
Create household
→ Invite spouse
→ Set household policy
→ Add accounts
→ Financial Pulse appears
```

## Journey 2 — Initial budget

```text
"Help me create an initial budget."
→ current finances retrieved
→ historical spending analyzed
→ proposed budget
→ review
→ approve
```

## Journey 3 — Budget diagnosis

```text
"Why am I always over budget?"
→ historical analysis
→ root causes
→ evidence
→ explanation
→ possible fixes
```

## Journey 4 — Next-month planning

```text
"Help me revise next month's budget."
→ known activities
→ missing cost questions only when necessary
→ proposed budget
→ impact
→ conversational adjustments
→ approval
```

## Journey 5 — Statement upload

```text
Drop PDF
→ processing
→ 126 imported
→ 2 review items
→ resolve
→ dashboard updates
```

## Journey 6 — Surprise expense

```text
"We have an unexpected $3,000 expense."
→ current liquidity
→ emergency fund
→ goals
→ debt
→ funding scenarios
→ validated recommendation
```

## Journey 7 — Windfall

```text
"We received a $10,000 bonus."
→ determine context/tax uncertainty
→ generate allocations
→ validate
→ compare alternatives
→ approve choice
```

## Journey 8 — Credit card

```text
"Which card should I use for this purchase?"
→ current card terms
→ rewards calculation
→ benefits
→ recommendation
→ evidence
```

## Journey 9 — FI

```text
"When can we retire?"
→ current financial state
→ assumptions
→ scenarios
→ Monte Carlo
→ sensitivities
→ explanation
```

---

# 27. Online Institution Integrations — Future / TBD

Direct connections to banks, credit-card issuers, loan providers, utilities, investment institutions, or aggregators are intentionally **postponed**. The delivery date is **TBD**.

When this work is eventually scheduled, it should reuse the existing provider-neutral ingestion contract and feed the same canonical processing path used by CSV/PDF imports. It must not create a second financial-data model.

Future integration work is expected to include, at minimum:

- provider/aggregator abstraction
- connection lifecycle
- credential/token handling
- incremental transaction retrieval
- balance retrieval
- duplicate/reconciliation integration
- connection health
- reconnect UX
- auditability
- provider outage handling

Until this capability is formally scheduled, the product should treat **CSV/PDF import as the authoritative ingestion mechanism**.

---

# 27. Definition of Done for the Entire Product

The system is directionally complete when:

1. Both spouses can use the application without technical assistance.
2. The household financial state is current based on the latest available imported data.
3. Statements can be uploaded with minimal user effort.
4. Normal ingestion is automatic.
5. Ambiguity goes to a clear Review Queue.
6. The dashboard explains household financial health in seconds.
7. The AI automatically retrieves relevant current financial context.
8. Budget creation is conversational and guided.
9. Budget diagnosis distinguishes behavior from an unrealistic budget.
10. Next-month budget planning supports known activities and conversational constraints.
11. Recommendations are evidence-backed and independently challenged.
12. Current facts are sourced and timestamped.
13. Public LLM access is minimized and privacy-controlled.
14. The application remains useful when external AI, research, or document-processing services are unavailable.
15. Persona selection changes framing but never financial truth.
16. Financial decisions are auditable.
17. Budget and financial-state changes require explicit approval.
18. Backups can be restored and tested.
19. Financial calculations have deterministic regression scenarios.
20. The application can be reproduced entirely from Docker-based deployment artifacts and remains fully useful without online institution integrations.

---

# 28. Recommended First Coding Session

Do not begin by implementing the full target architecture.

Start with:

```text
1. Initialize repository
2. Create AGENTS.md
3. Create PRODUCT_BUILD_CONTRACT.md
4. Create Docker Compose baseline
5. Create web/api/worker shells
6. Create PostgreSQL
7. Create Keycloak
8. Establish Money primitive
9. Implement Slice 1 domain
10. Seed a fictional household
11. Build FinancialSnapshot
12. Build Financial Pulse
13. Run first spouse usability test
```

The first meaningful milestone is not:

> "The AI works."

It is:

> "A household can open the application and understand its current financial situation within 30 seconds."

The next milestone is:

> "A spouse can drop in a bank, credit-card, loan, or utility CSV/PDF and the system handles almost everything automatically."

Then:

> "I can ask the advisor a question without explaining my finances."

Then:

> "The advisor can help us create and revise our budget conversationally."

Then:

> "The advisor can research, challenge, and explain a financial recommendation."

That progression keeps the prototype fast while preserving the architecture needed for a serious privacy-first household financial advisor.
